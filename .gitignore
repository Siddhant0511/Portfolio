# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# raw project source material (large, contains private documents)
Porjects case files/
RESUME AND OTHER FILES/

# `npx skills add` keeps the skill itself in .agents/skills (committed) and drops a
# symlink here pointing at an absolute local path, which is machine-specific.
.claude/skills/design-taste-frontend
